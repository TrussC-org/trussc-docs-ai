node_modules/
# chunks.embedded.json IS committed (runtime artifact → clone-and-go on the server;
# rebuilding it needs the source repos + Ollama, which the server may not have).
chunks.jsonl
# dev/session artifacts
eval-report.txt
MORNING.md
*.log
